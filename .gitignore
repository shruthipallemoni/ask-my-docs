# Data artifacts — regenerated by the pipeline, shouldn't live in git
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.venv-eval/
.github/

# Env / secrets (none needed for this project, but keep the habit)
.env

# OS
.DS_Store